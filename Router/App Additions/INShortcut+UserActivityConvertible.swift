import Intents

//extension INShortcut: UserActivityConvertible {
//
    // In the future we should add support for `INShortcut`s initialized via `INIntent`
    // or make the whole initialization concept failable/throwing.
    // Still not sure which would be the best approach to take so I'm ignoring it for now.

//    var userActivity: NSUserActivity {
//        return self.userActivity!
//    }
//
//}

//extension INIntent: UserActivityConvertible {
//
//    var activity: NSUserActivity {
//        let userActivity = NSUserActivity(activityType: self.identifier!)
//        userActivity.title = self.intentDescription
//        userActivity.userInfo = self.userInfo
//
//        return userActivity
//    }
//
//}
